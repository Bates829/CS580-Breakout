
/*
export default class Ball{
  constructor(){
    this.x2 = 2;
    this.y2 = -2;
    this.ballRadius = 10;
  }

  //Draw ball on to canvas
  drawBall(ctx){
    ctx.beginPath();
    ctx.arc(this.x, this.y, this.ballRadius, 0, Math.PI * 2);
    ctx.fillStyle = 'blue';
    ctx.fill();
    ctx.closePath();
  }
}
*/
